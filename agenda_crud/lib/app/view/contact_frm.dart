import 'package:flutter/material.dart';

class ContactFrm extends StatelessWidget {
  const ContactFrm({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Cadastro de Contatos:")),
    );
  }
}
